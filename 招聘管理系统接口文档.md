# 招聘管理系统接口文档

## 1. 通用约定

- 前端通过 Nginx 使用 `/api` 前缀访问接口，例如前端请求 `/api/admin/jobs`，后端实际接收 `/admin/jobs`。
- 除 `/auth/code` 和 `/auth/login` 外，接口都需要在请求头携带 Token：

```http
token: JWT_TOKEN
Content-Type: application/json
```

- 通用成功响应：

```json
{
  "code": 1,
  "msg": null,
  "data": {}
}
```

- 分页响应：

```json
{
  "code": 1,
  "msg": null,
  "data": {
    "total": 7,
    "records": []
  }
}
```

- 职位状态：`0` 待审核，`1` 已通过/招聘中，`2` 已驳回，`3` 已下架。
- 投递状态：`0` 待沟通，`1` 已查看，`2` 邀约面试，`3` 面试通过，`4` 发送 Offer，`5` 已入职，`6` 不合适，`7` 已拒绝。
- 用户角色：`0` 求职者，`1` HR，`2` 管理员。

## 2. 管理端接口

### 2.1 管理员登录验证码

`POST /auth/code`

请求体：

```json
{
  "account": "admin",
  "role": 2
}
```

返回：验证码字符串，例如 `"123456"`。当前为开发验证码，同时写入 Redis，过期时间 5 分钟。

### 2.2 管理员登录

`POST /auth/login`

请求体：

```json
{
  "account": "admin",
  "code": "123456",
  "role": 2
}
```

返回：

```json
{
  "userId": 1,
  "username": "admin",
  "role": 2,
  "token": "JWT_TOKEN"
}
```

JWT 中包含 `userId` 和 `role`。

### 2.3 管理员仪表盘

`GET /admin/dashboard`

请求参数：无。

返回：`totalJobs`（全部职位总数）、`activeJobs`（招聘中职位数）、`pendingJobs`（待审核职位数）、用户数、HR 数、求职者数、累计投递数等 `DashboardVO` 数据。

### 2.4 分页查询全部岗位

`GET /admin/jobs`

请求参数：

| 参数 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| page | int | 否 | 页码，默认 1 |
| pageSize | int | 否 | 每页数量，默认 10 |
| keyword | string | 否 | 职位名称、类别或企业名称 |
| city | string | 否 | 城市 |
| category | string | 否 | 职位类别 |
| salaryMin | int | 否 | 薪资下限 |
| salaryMax | int | 否 | 薪资上限 |
| status | int | 否 | 职位状态 |

返回 `PageResult`，`records` 为职位列表，包含职位信息、企业信息、投递人数、浏览次数、审核备注和状态。

### 2.5 查看职位详情

`GET /admin/jobs/{id}`

前端发送：路径参数 `id`。

返回一个完整 `JobVO`，包括：

```json
{
  "id": 5,
  "title": "运维开发工程师",
  "companyName": "星云科技",
  "city": "北京",
  "salaryMin": 22,
  "salaryMax": 38,
  "workExperience": "3-5年",
  "education": "本科",
  "jobDescription": "职位描述",
  "requirement": "任职要求",
  "benefits": "五险一金,双休",
  "deliveryCount": 0,
  "viewCount": 30,
  "status": 0,
  "auditRemark": null
}
```

### 2.6 分页查询待审核岗位

`GET /admin/reviews`

请求参数：与 `/admin/jobs` 的分页和搜索参数相同。

后端固定增加 `status=0` 条件，前端不应自行把全部岗位过滤成待审核岗位。

返回：只包含待审核岗位的 `PageResult`。`total` 是数据库中符合搜索条件的待审核岗位总数。

### 2.7 审核通过或拒绝岗位

`POST /admin/reviews/{id}`

请求体：

```json
{
  "status": 1,
  "remark": "审核通过"
}
```

`status=1` 表示通过，`status=2` 表示拒绝。

返回：空数据成功响应。审核后岗位状态和审核备注会更新，审核页下一次查询不会再显示该岗位。

### 2.8 用户列表

`GET /admin/users`

请求参数：

| 参数 | 类型 | 说明 |
|---|---|---|
| role | int | 可选，0 求职者、1 HR、2 管理员 |
| keyword | string | 可选，按账号关键字查询 |

返回：用户数组，包含用户 ID、账号、手机号、邮箱、角色和账户状态。

### 2.9 启用或禁用用户

`PUT /admin/users/{id}/status?status=0`

请求参数：

| 参数 | 类型 | 说明 |
|---|---|---|
| id | long | 用户 ID |
| status | int | `1` 启用/正常，`0` 禁用 |

返回：空数据成功响应。`status=2` 的已注销用户不允许通过此接口恢复。

## 3. HR 端接口

### 3.1 HR 登录验证码与登录

验证码：`POST /auth/code`

```json
{
  "account": "hr.zhang@tech.com",
  "role": 1
}
```

登录：`POST /auth/login`

```json
{
  "account": "hr.zhang@tech.com",
  "code": "123456",
  "role": 1
}
```

返回 `userId`、`username`、`role`、`token`。

### 3.2 HR 仪表盘

`GET /hr/dashboard`

返回当前 HR 的职位数、投递数、面试数和待审核职位数等 `DashboardVO` 数据。

### 3.3 HR 职位分页查询

`GET /hr/jobs` 或 `GET /hr/jobs/page`

请求参数：

```text
page, pageSize, keyword, city, category, salaryMin, salaryMax, status
```

后端自动限制为当前登录 HR 发布的职位。返回 `PageResult<JobVO>`。

### 3.4 发布职位

`POST /hr/jobs`

请求体：

```json
{
  "companyId": 1,
  "title": "Java 后端开发工程师",
  "category": "技术",
  "salaryMin": 20,
  "salaryMax": 35,
  "city": "北京",
  "workExperience": "3-5年",
  "education": "本科",
  "jobDescription": "负责后端系统开发",
  "requirement": "熟悉 Spring Boot",
  "benefits": "五险一金,双休"
}
```

返回：空数据成功响应。未传状态时后端默认创建为待审核。

### 3.5 修改职位

`PUT /hr/jobs`

请求体：与发布职位相同，必须带 `id`。

返回：空数据成功响应。只能修改当前 HR 自己的职位。

### 3.6 上下架职位

`POST /hr/jobs/{id}/status?status=3`

前端发送路径参数 `id` 和查询参数 `status`。常用值：`1` 招聘中，`3` 已下架。

返回：空数据成功响应。

### 3.7 HR 删除职位

`DELETE /hr/jobs?ids=5&ids=6`

前端发送一个或多个 `ids`。存在投递记录的职位不能删除。

返回：空数据成功响应。

### 3.8 HR 查看职位详情

`GET /hr/jobs/{id}`

返回当前 HR 自己职位的完整 `JobVO`。

### 3.9 HR 查看投递列表

`GET /hr/applications`

请求参数：

| 参数 | 类型 | 说明 |
|---|---|---|
| jobId | long | 可选，限定某个职位 |
| status | int | 可选，投递状态 |

返回 `PageResult<DeliveryVO>`，包含候选人、简历、职位、企业、投递时间和投递状态。

### 3.9.1 HR 查看单条投递详情

`GET /hr/applications/{id}`

返回当前 HR 有权限查看的 `DeliveryVO`，用于投递进度弹窗和简历信息展示。

### 3.10 HR 推进投递状态

`PUT /hr/applications/{id}/status`

请求体：

```json
{
  "status": 1,
  "remark": "已查看简历"
}
```

状态必须按顺序推进，不能跨状态；`6` 不合适、`7` 已拒绝可作为终止状态。
HR 在 `1` 已查看阶段可以直接选择 `6` 不合适；在 `2` 邀约面试阶段通过面试操作选择通过或不通过，分别进入 `3` 面试通过或 `6` 不合适。

当目标状态为 `2` 邀约面试时，必须先调用创建面试邀约接口，不能直接修改投递状态。

返回：空数据成功响应，同时向求职者创建通知。

### 3.11 HR 查看面试列表

`GET /hr/interviews`

请求参数：无。

返回当前 HR 的 `PageResult<InterviewVO>`。
HR 查询只返回关联用户 `status=1`（正常）的面试记录。

### 3.11.1 HR 创建面试邀约

`POST /hr/interviews`

请求体：

```json
{
  "deliveryId": 12,
  "interviewer": "张三",
  "interviewTime": "2026-08-25T14:00:00",
  "duration": 60,
  "location": "公司会议室",
  "addressDetail": "北京市朝阳区某大厦 8 层",
  "contactPerson": "李四",
  "contactPhone": "13800000000",
  "remark": "请携带身份证"
}
```

只有状态为 `1` 已查看的投递可以创建邀约。创建成功后，投递状态自动变为 `2` 邀约面试，面试状态为 `0` 待确认。

### 3.11.2 HR 修改面试邀约

`PUT /hr/interviews/{id}`

请求体字段与创建接口相同。待确认或已确认的面试可以修改，已拒绝或已完成的面试不能修改。

### 3.11.3 HR 取消面试邀约

`DELETE /hr/interviews/{id}?remark=HR%20%E5%B7%B2%E5%8F%96%E6%B6%88%E9%9D%A2%E8%AF%95%E9%82%80%E7%BA%A6`

取消后面试状态变为 `4` 已取消，并通知求职者；求职者主动拒绝时状态为 `2` 已拒绝。

### 3.11.4 HR 完成面试

`PUT /hr/interviews/{id}/status`

请求体：

```json
{
  "status": 3,
  "remark": "面试完成"
}
```

只有求职者已确认的面试可以标记完成。完成后，投递状态自动变为 `3` 面试通过。

### 3.12 HR 企业资料

查询：`GET /hr/company`

返回当前 HR 绑定的企业对象。

修改：`PUT /hr/company`

请求体可包含：

```json
{
  "companyName": "星云科技",
  "shortName": "星云",
  "industry": "互联网",
  "companySize": "100-499人",
  "address": "北京市朝阳区",
  "website": "https://example.com",
  "description": "企业简介"
}
```

返回：空数据成功响应。

### 3.13 HR 通知

查询：`GET /hr/notifications`

返回当前 HR 的通知数组。
与投递或面试相关的通知，只返回关联求职者 `status=1`（正常）的通知；禁用或注销用户的相关通知会被隐藏。

标记已读：`PUT /hr/notifications/{id}/read`

返回：空数据成功响应。

## 4. 求职者端接口

### 4.1 求职者登录验证码与登录

验证码：`POST /auth/code`

```json
{
  "account": "13800000010",
  "role": 0
}
```

登录：`POST /auth/login`

```json
{
  "account": "13800000010",
  "code": "123456",
  "role": 0
}
```

返回 `userId`、`username`、`role`、`token`。

### 4.2 职位分页查询

`GET /candidate/jobs`

请求参数：

```text
page, pageSize, keyword, city, category, salaryMin, salaryMax
```

后端固定只查询 `status=1` 的招聘中职位，返回 `PageResult<JobVO>`。

### 4.3 热门职位

`GET /candidate/jobs/hot?city=北京`

请求参数：可选 `city`。

返回最多 20 个热门 `JobVO`。Redis Key 规则为 `job:hot:{city}`，缓存 30 分钟。

### 4.4 职位详情

`GET /candidate/jobs/{id}`

返回招聘中的完整 `JobVO`，同时增加职位浏览次数。

### 4.5 简历列表

`GET /candidate/resumes`

返回当前求职者的简历数组。当前已有查询接口，简历字段直接来自 `resume` 表。

### 4.6 投递简历

`POST /candidate/deliveries`

请求体：

```json
{
  "jobId": 1,
  "resumeId": 1
}
```

后端校验职位是否招聘中、简历是否属于当前用户、是否重复投递；成功后创建状态为 `0` 的投递记录。

返回：空数据成功响应。

### 4.7 查看我的投递

`GET /candidate/deliveries?status=1`

请求参数：可选 `status`。

返回 `PageResult<DeliveryVO>`，包含职位、企业、简历、投递时间和当前状态。

### 4.7.1 求职者查看单条投递详情

`GET /candidate/deliveries/{id}`

只能查看当前求职者自己的投递记录。

### 4.8 求职者处理面试邀约

查询：`GET /candidate/interviews`

返回当前求职者的 `PageResult<InterviewVO>`。

确认或拒绝：`PUT /candidate/interviews/{id}/status`

请求体：

```json
{
  "status": 1,
  "remark": "确认参加面试"
}
```

面试状态：`0` 待确认，`1` 已确认，`2` 已拒绝，`3` 已完成，`4` 已取消。

### 4.9 求职者通知

查询：`GET /candidate/notifications`

返回当前求职者通知数组。

标记已读：`PUT /candidate/notifications/{id}/read`

返回：空数据成功响应。

## 5. 当前仍为静态数据或尚未完成

以下内容目前没有完整后端接口，前端仍是占位或未接入状态：

1. 求职者简历新增、编辑、删除、设置默认简历：已实现，前端已接入 `POST /candidate/resumes`、`PUT /candidate/resumes/{id}`、`DELETE /candidate/resumes/{id}`、`PUT /candidate/resumes/{id}/default`。
2. HR 面试邀约创建、编辑、取消、完成，以及求职者接受/拒绝面试邀约，已接入后端接口。
3. 管理员用户编辑和注销：目前未实现；用户启用、禁用接口已经接入。
4. 短信和邮件验证码真实发送：当前验证码固定为开发值 `123456`，会写入 Redis，尚未接入短信或邮件服务。
5. 部分仪表盘中的“审核提示”等说明文字是前端固定文案，不是业务数据。
6. HR 和求职者投递列表的“查看进度”已接入单条投递详情接口，简历编辑接口也已补齐。
7. 热门岗位接口采用缓存旁路模式：先查 Redis，未命中则查 MySQL，再回写 Redis，Redis 不可用时自动降级到 MySQL。
7. 前端当前已接入的岗位列表、职位详情、审核、投递、状态推进、企业资料、通知和仪表盘数据均来自后端接口，不再使用原来的职位静态数组。

## 6. 本次新增和修改的接口

- 新增 `GET /admin/jobs`：管理员分页查询全部岗位。
- 新增 `GET /admin/jobs/{id}`：管理员查看职位详情。
- 修改 `GET /admin/reviews`：固定只查询 `status=0` 的待审核岗位。
- 前端新增管理员独立“职位管理”页面、分页、职位详情弹窗。
- 前端审核页增加“查看详情”“审核通过”“拒绝”三个操作。
