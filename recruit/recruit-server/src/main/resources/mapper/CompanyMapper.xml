<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.recruit.mapper.CompanyMapper">

    <select id="getById" resultType="com.recruit.entity.Company">
        select id, hr_id, company_name, short_name, industry, company_size, logo, address, website, description, status, create_time, update_time
        from company
        where id = #{id}
    </select>

    <select id="getByHrId" resultType="com.recruit.entity.Company">
        select id, hr_id, company_name, short_name, industry, company_size, logo, address, website, description, status, create_time, update_time
        from company
        where hr_id = #{hrId}
        limit 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into company (
            hr_id, company_name, short_name, industry, company_size, logo,
            address, website, description, status, create_time, update_time
        ) values (
            #{hrId}, #{companyName}, #{shortName}, #{industry}, #{companySize}, #{logo},
            #{address}, #{website}, #{description}, #{status}, #{createTime}, #{updateTime}
        )
    </insert>

    <update id="update">
        update company
        <set>
            <if test="companyName != null">company_name = #{companyName},</if>
            <if test="shortName != null">short_name = #{shortName},</if>
            <if test="industry != null">industry = #{industry},</if>
            <if test="companySize != null">company_size = #{companySize},</if>
            <if test="logo != null">logo = #{logo},</if>
            <if test="address != null">address = #{address},</if>
            <if test="website != null">website = #{website},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        where id = #{id}
    </update>
</mapper>
