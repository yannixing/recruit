# Recruit 前端第一阶段设计

## 1. 现状判断

`nginx-1.20.2/html/sky` 是已经构建完成的 Vue 静态产物，目录中没有 `package.json`、Vue 源码或可直接运行的前端工程。虽然 source map 仍然保留了部分苍穹外卖源码，但继续修改压缩后的 bundle 会让后续维护困难。

因此第一阶段新增独立静态入口：

```text
nginx-1.20.2/html/recruit/
  index.html
  styles.css
  app.js
```

原来的 `html/sky` 保留作为回退版本。

## 2. 页面信息架构

### 求职者

- 职位发现：关键词、城市筛选、职位卡片、薪资和职位详情。
- 我的投递：投递记录、状态时间线、面试阶段、Offer 阶段。
- 我的简历：多份简历、默认简历、简历概要。
- 面试邀约：面试时间、地点、候选人确认状态。
- 消息中心：投递进度、面试邀约、系统通知。

### HR

- 招聘概览：在招职位、待处理投递、本周面试、待审核职位。
- 职位管理：发布、编辑、筛选、职位状态和投递人数。
- 投递管理：候选人、简历、投递状态推进。
- 面试安排：面试日程和候选人安排。
- 企业资料：企业名称、行业、规模、地址和企业简介。
- 消息中心：招聘流程中的通知。

### 管理员

- 数据概览：平台职位、待审核职位、注册用户、累计投递。
- 职位审核：通过或驳回待审核职位。
- 用户管理：查看账户、角色和状态。

## 3. 数据映射

前端演示数据来自 `database/Script.sql`：

- `company` 为企业展示信息来源。
- `job` 为职位卡片、职位详情和职位管理来源。
- `resume` 为投递时的简历选择来源。
- `delivery` 为投递列表和状态流转来源。
- `interview` 为面试日程来源。
- `notification` 为消息中心来源。

职位状态使用数据库定义：

```text
0 待审核
1 已通过 / 招聘中
2 已驳回
3 已下架
```

投递状态使用数据库定义：

```text
0 待沟通 -> 1 已查看 -> 2 邀约面试 -> 3 面试通过 -> 4 发送 Offer -> 5 已入职
6 不合适、7 已拒绝为终止状态
```

## 4. 后端接口替换点

当前 `app.js` 使用本地数组以便在后端接口尚未调整时直接查看页面。后续只需要把本地数据层替换成请求函数，页面结构不需要重做。

建议接口前缀：

```text
/api/auth/login
/api/candidate/jobs
/api/candidate/jobs/{id}
/api/candidate/resumes
/api/candidate/deliveries
/api/candidate/interviews
/api/candidate/notifications
/api/hr/jobs
/api/hr/applications
/api/hr/interviews
/api/hr/company
/api/hr/notifications
/api/admin/reviews
/api/admin/users
```

## 5. Nginx

Nginx 根目录切换到 `html/recruit`，保留 `/api/` 反向代理入口。后端接口尚未修改，因此当前页面默认使用演示数据，不依赖后端可用性。
